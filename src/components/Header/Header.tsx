import { Container, SiteName, SiteTagline } from './Header.tw'

export const Header = () => (
  <Container>
    <h1>
      <SiteName>Humorous</SiteName>
    </h1>
    <SiteTagline>The only one color generator you will ever need</SiteTagline>
  </Container>
)
